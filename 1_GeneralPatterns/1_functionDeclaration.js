// antipattern
function getData() {
};

//better
var getData = function() {
};

//named function expresion (can be called recursive + provide debugger explicit function name)
var getData = function gatData(){
};

//better - named expresion with +F
var getData = function getDataF(){
};