// it's good to declare the modules your code relies on at the top
var myFunction = function() {
    // dependencies
    var event = YAHOO.util.Event,
        dom = YAHOO.util.dom;
    // use event and dom variables
    // for the rest of the function...
};
